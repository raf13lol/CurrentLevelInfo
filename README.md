# CurrentLevelInfo

## bepinex 6 mod once more (plus bepinex 5 seems dead on latest beta at the time of writing)

this is simple enough, just writes the current level's information to a text file to use in streaming or something